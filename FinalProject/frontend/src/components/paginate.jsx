import React from 'react'
import { Paginator } from 'react-bootstrap'
import { LinkContainer } from 'react-router-bootstrap';


function paginate(pages, page, name ='', isAdmin = false ) {
    return (
        <div>
            
        </div>
    )
}

export default paginate
